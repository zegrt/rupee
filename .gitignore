.DS_Store
.idea/
*.iml
.gradle/
local.properties
build/
**/build/
captures/
.swp

*.jks
*.keystore

# Local IDE / agent tooling — never tracked
.agents/
.cursor/
.github/
.jetro/
.kotlin/
.mcp.json
.windsurfrules

# Jetro auto-generated session prompts (regenerated on each boot)
AGENT.md
CLAUDE.md

# Local Gradle daemon JVM config (depends on the dev's installed JDK)
gradle/gradle-daemon-jvm.properties

# Parked design exploration artefacts (Aviate vs vwfndr APKs from the
# design/aviate-vs-vwfndr branch — large binaries, not part of the build).
design-research/
