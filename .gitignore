.DS_Store
.idea/

# Built APKs copied into design-research/apks for sharing — never committed
design-research/apks/

*.iml
.gradle/
local.properties
build/
**/build/
captures/
.swp

*.jks
*.keystore

# Local IDE / agent tooling — never tracked
.agents/
.cursor/
.github/
.jetro/
.kotlin/
.mcp.json
.windsurfrules

# Jetro auto-generated session prompts (regenerated on each boot)
AGENT.md
CLAUDE.md

# Local Gradle daemon JVM config (depends on the dev's installed JDK)
gradle/gradle-daemon-jvm.properties
