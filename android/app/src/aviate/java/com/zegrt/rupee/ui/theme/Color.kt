package com.zegrt.rupee.ui.theme

import androidx.compose.ui.graphics.Color

// Aviate flavor palette — calm, premium, identity-coded.
// Pastels + one deep blue. Negative is *warm peach*, never harsh red — the
// product refuses to scold the user. Everything saturated is muted ~10–20%.

val AviInk = Color(0xFF0E1B33)            // deep navy — primary text
val AviPaper = Color(0xFFF6F7FB)          // off-white background
val AviCloud = Color(0xFFE5ECF9)          // pale blue — primary surface
val AviSky = Color(0xFF2E5BFF)            // hero blue — actions
val AviSkyDim = Color(0xFF7A93D9)         // muted blue — secondary text
val AviRose = Color(0xFFF6D9E4)           // dusty pink surface
val AviPeach = Color(0xFFFAE0D2)          // warm peach — negative tile
val AviPeachInk = Color(0xFFB44415)       // peach-on-peach text
val AviSand = Color(0xFFEDE7DC)           // cream surface variant
val AviMoss = Color(0xFFD5E5D5)           // mint surface — positive
val AviMossInk = Color(0xFF205F2C)
val AviHairline = Color(0xFFD8DFEC)       // soft divider
val AviMuted = Color(0xFF6E7A93)          // body muted text

// Dark mode keeps the warm/calm identity but flips bg to a deep navy-ink
// so the product stays sentimental even at night. Cards lift with the same
// pastels at lower saturation.
val AviInkDark = Color(0xFF0A1124)
val AviSurfaceDark = Color(0xFF141D36)
val AviSurfaceDarkRaised = Color(0xFF1C2745)
val AviWarmGreyDark = Color(0xFFE3E8F2)
val AviHairlineDark = Color(0xFF273252)
